public class SystConv{
    
    //Attributs
    private Array <String> connectedUsersL;

    //Constructeur
    public void SystConv(Array <String> connectedUsersL){
        this.connectedUsersL;
    }

    //Methodes
    private void askPseudo(Struct U2 user){}

    private void askPwd(Struct U2 user){}

    private void createUser(int id, String pwd){}

    private void sendM(String msg, Struct U2 user){}

    private void updateConnectedUL(int id){}

    private Boolean convExists(){
        exist = new Boolean();
        return exist;
    }

    private <Struct Conv> getHistorique(Struct U2 user1, Struct U2 user2){
        hist = new StructConv();
        return hist;
    }

    private void updateHistConv(Struct Conv listNewM){}

    private void notifyModifiedPseudo(Struct U2 user){}

    private int findId(Struct U2 user){
        id = 0;
        return id;
    }

}